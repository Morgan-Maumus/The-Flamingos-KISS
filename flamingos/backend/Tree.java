public class Tree {

    private boolean empty;
    private int treeSize, treeHeight;

    //Returns the size of a given tree
    public static int getSize(Tree tree) {

        return 0;
    }


    //Returns the height of a given tree
    public static int getHeight(Tree tree) {

        return -1;
    }


    //Returns whether a given tree is empty or not
    public static boolean isEmpty(Tree tree) {

        return false;
    }

    //Inserts a node into a tree
    public static void insertNode(Tree tree, Node node) {

        return;
    }


    //Deletes node from tree
    public static void deleteNode(Tree tree, Node node) {

        return;
    }

    //Searches tree for a node
    public static int searchTree(Tree tree, Node node) {

        return -1;
    }


    //Deletes a tree along with its nodes
    public static void clearTree(Tree tree) {

        return;
    }

}